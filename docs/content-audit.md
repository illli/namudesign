# 上线前公开信息边界

本文记录 NAMU DESIGN 官网首版上线时可以公开的事实、必须统一的域名规则，以及仍需确认或禁止沿用的内容。页面文案、SEO 元信息、结构化数据、站点地图和 `llms.txt` 均应遵守同一边界。

## 域名与索引规则

- 唯一主域为 `https://namu.design`。
- `https://www.namu.design` 及其所有路径应以永久重定向跳转到对应的无 `www` 地址。
- 页面 canonical、Open Graph URL、结构化数据 URL、站点地图、robots 和 `llms.txt` 必须统一使用无 `www` 域名。
- 中文和英文页面分别使用自指 canonical，并配置对应的 `hreflang` 互链；英文页不得 canonical 到中文页。

## 当前允许公开的信息

### 品牌与定位

- 品牌名：`NAMU DESIGN` / `呐姆设计`。
- 可使用 Figma 已确认的定位：NAMU design 是一个跨媒介的视觉沟通团队，关注视觉设计在不同领域中的沟通功能与特性。
- 未经确认，不把“SEO/GEO 优化”“上海产品体验设计公司”或“高保真原型工作室”写成正式核心定位或服务承诺。

### 联系方式

- 邮箱：`hi@namu.design`。
- 电话：`18516592921`；需要国际格式时使用 `+86 18516592921`，电话链接使用 `tel:+8618516592921`。
- 首页可以显示 `18516592921（周）`；Info 页按设计显示电话号码即可。
- 地址：`上海市徐汇区淮海中路1610弄1号`。
- 英文地址：`No. 1, Lane 1610, Middle Huaihai Road, Xuhui District, Shanghai, China`。

### 团队

以下团队姓名和英文名可以按 Figma 原文公开，排序不得擅自调整：

1. 周展宇 / 11
2. 潘梦雨 / DIO
3. 马文超 / Jerry
4. 吕天宇 / TT
5. 李江南 / Jiangnan
6. 钟秋月 / Monte
7. 熊文梅 / Maime
8. 左定明 / zuozuo

### 案例事实

- 方太案例可以公开：项目名“方太小程序首页改版”、客户 `FOTILE`、年份 `2025`。
- 第二个首发案例是 `FARFETCH China`，应以 Figma 中该案例的标题、年份、客户和参与信息为准。
- 案例页面可以描述设计稿中可见的项目背景、团队角色和交付物；没有客户确认或证据时，不得把推断写成项目成果。

## 暂不展示或禁止沿用

### ICP 备案号

- Figma 中出现的 `沪ICP备281042902190号` 暂不展示。
- 该号码必须先在工信部备案系统核验域名、备案主体和准确号码。
- 核验通过后，才可放入全站页脚并链接至 `https://beian.miit.gov.cn/`。

### 错误案例内容

- 禁止沿用旧仓库中的 `LV` 案例名称、页面、路径、正文、结构化数据、图片替代文字、SEO 关键词及 `llms.txt` 描述。
- 旧文件名 `lv-figma.png` 不能作为 LV 项目证据；该图片实际是多项目画板总览，应按真实内容重新命名和描述，或不用于公开案例。
- 站点地图和内链不得保留 `/cases/lv.html`。

### 无证据主张

下列说法在获得负责人或客户确认前不得公开：

- “成员人均十年经验”。
- “长期服务国内外知名企业”。
- “为顶尖品牌打造极致数字体验”。
- 方太或 FARFETCH 项目带来的业务效果、效率提升、转化提升或客户决策成效。
- Figma 未明确列出的设计范围、交付范围或正式服务能力。
- 将任何未在案例元数据中确认的品牌写入结构化数据的 `client` 字段。

## 上线前最终核对

- 联系方式、地址、团队名单和案例元数据在中英文页面、SEO 元信息与结构化数据中一致。
- FARFETCH China 已完全替换旧 LV 内容，且全仓库不再出现误导性的 LV 案例信息。
- ICP 备案号保持隐藏，直至完成官方核验。
- 无 `www` canonical 与 `www` 跳转在公网逐路径验证通过。
- 所有项目成果陈述均能追溯到 Figma、客户确认或可公开的项目证据。
