# 重要决策

## 2026-09-01

- 采用无后端依赖的静态站点架构，减少服务器运行时与维护面。
- 以 Figma 的 4 个顶层页面为首期官网范围：首页、Info、方太案例、FARFETCH China 案例。
- 中英文使用独立 URL，而非仅在浏览器中替换文字，以支持分享与 SEO。
- 主题支持系统偏好与手动覆盖，选择保存在浏览器本地。
- 所有 Figma 资产下载到项目并随站点部署，禁止生产环境引用临时 Figma 链接。
- 部署采用版本目录加软链接切换，先备份再上线，保留回滚能力。
- 私钥不复制进仓库，不写入日志，不上传服务器。
- Figma 中的备案号暂不发布；该号码格式可疑，需由用户从备案管理后台提供可核验的正式号码后再补充。
- GitHub 中原有的 LV 案例和整版 Figma 拼图不进入新版发布范围，避免展示未获设计稿支持的内容或泄露其他项目画板。
- 生产发布只上传 `dist/`，源代码、验收截图和私钥不进入 Web 根目录。
- 页头视觉严格保持 Figma 原稿，只显示 Logo 与原始 INFO 字形；语言和亮暗模式入口统一放在页脚。
- 设计稿未出现的页面内容不擅自删除或新增，先向用户确认；用户已确认删除首页设计稿外的两张 WORK 案例卡片。
- 正文优先使用 Figma 对应的 Source Sans 3 / PingFang SC 字体组合，并自托管 Source Sans 3 与 Source Han Sans CN 作为跨平台回退。
- 首页作品图采用 28 秒线性无缝横向循环；页面首屏由页头之外的 `.home-stage` 按 `100svh` 计算，能力标签从下一屏开始。
- 首页第 1、2 张图分别链接方太和 FARFETCH China；第 3、4 张保持非链接占位，等正式案例与 URL 后再开放。
- 中文版团队名单只显示中文姓名与中文职位，中文页脚地址只显示中文地址；英文名、英文职位与英文地址只保留在英文版。
- 英文首页 Capabilities 在 640px 以下使用 78px 高的居中标签；1024px 以上收紧列间距并使用 18px 单行文字，避免本地化文案溢出，同时不改变中文版样式。
- 首页可点击案例封面直接复用详情页已展示的真实素材：方太采用 `case-01-01.png`，FARFETCH 采用 `fifi-01.png`；不再使用与案例无关的首页占位图。
- 页脚地址严格跟随页面语言：中文 URL 只显示中文地址，英文 URL 只显示英文地址。
- 团队职位严格跟随页面语言；姓名不翻译：中文 Info 显示中文姓名与中文职位，英文 Info 显示“中文姓名 + 已提供的英文名/昵称”与英文职位。
- 客户 Logo 优先使用完整真实品牌资产；不得让 SVG 画布留白或损坏的裁切范围压缩实际字标。
- 页脚辅助控制在所有断点置于底行右侧：语言保持文字，主题使用 Heroicons 月亮/太阳线性图标；亮色显示月亮，暗色显示太阳，表达点击后的目标模式。
